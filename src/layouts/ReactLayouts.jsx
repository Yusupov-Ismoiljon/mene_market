// import Home from '../pages/Home';
import { Outlet } from 'react-router-dom';

const ReactLayouts = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
}

export default ReactLayouts